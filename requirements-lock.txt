# Exact dependency versions for reproducible CI builds.
# Generated from the installed environment — update with:
#   pip freeze > requirements-lock.txt
#
# For development installs, use:
#   pip install -e ".[dev]"
#
# For locked CI installs, use:
#   pip install -r requirements-lock.txt && pip install -e . --no-deps

# Runtime
typer==0.15.4
anthropic==0.49.0
slack-sdk==3.35.0
rich==13.9.4
pydantic==2.13.3
markdown-it-py==3.0.0

# Dev / test
pytest==8.3.5
pytest-cov==6.0.0
pytest-asyncio==0.25.3
pytest-mock==3.14.0
hypothesis==6.130.5
ruff==0.9.10
